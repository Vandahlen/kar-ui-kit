import { StyleProp, ViewStyle } from 'react-native';

/** Shared props for the traced Karappen icon set. */
export interface IconProps {
  size?: number;
  color: string;
  style?: StyleProp<ViewStyle>;
}
